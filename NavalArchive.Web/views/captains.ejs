<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header') %>
  <main class="app-shell">
    <div class="container py-4">
      <h1 class="mb-4">Captains</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-warning"><%= error %></div>
      <% } %>
      <div class="row g-4">
        <% if (captains && captains.length > 0) { %>
          <% captains.forEach(function(c) { %>
            <div class="col-md-6 col-lg-4">
              <div class="card h-100">
                <% if (c.imageUrl && c.imageUrl.startsWith('http')) { %>
                  <img src="<%= c.imageUrl %>" class="card-img-top" alt="<%= c.name %>" style="height: 200px; object-fit: cover;" onerror="this.onerror=null;this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22200%22 height=%22200%22%3E%3Crect fill=%22%236c757d%22 width=%22200%22 height=%22200%22/%3E%3Ctext fill=%22%23fff%22 x=%22100%22 y=%22100%22 dominant-baseline=%22middle%22 text-anchor=%22middle%22 font-size=%2212%22%3ENo photo%3C/text%3E%3C/svg%3E';">
                <% } else { %>
                  <div class="card-img-top img-placeholder d-flex align-items-center justify-content-center" style="height: 200px;">
                    <span><%= c.name %></span>
                  </div>
                <% } %>
                <div class="card-body">
                  <h5 class="card-title"><%= c.name %></h5>
                  <p class="card-text text-muted mb-1"><%= c.rank %></p>
                  <p class="card-text small mb-2"><%= c.serviceYears %> years service · <%= c.shipCount %> ship<%= c.shipCount !== 1 ? 's' : '' %> commanded</p>
                  <a href="/captains/<%= c.id %>" class="btn btn-outline-dark btn-sm">View Profile</a>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <div class="col-12 text-center text-muted py-5">No captains found.</div>
        <% } %>
      </div>
    </div>
  </main>
  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
