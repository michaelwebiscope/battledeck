<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header') %>
  <main class="app-shell">
    <div class="container py-4">
      <h1 class="mb-4">Photo Gallery</h1>
      <p class="text-muted mb-4">Archival photographs from the fleet. Click "View Full Size" to load the high-resolution image (triggers backend memory cache).</p>
      <div class="row g-4">
        <% if (ships && ships.length > 0) { %>
          <% ships.forEach(function(ship) { %>
            <div class="col-md-4 col-lg-3">
              <div class="card h-100">
                <% if (ship.imageUrl && ship.imageUrl.startsWith('http')) { %>
                  <img src="<%= ship.imageUrl %>" class="card-img-top" alt="<%= ship.name %>" style="height: 180px; object-fit: cover;" onerror="this.onerror=null;this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22200%22 height=%22180%22%3E%3Crect fill=%22%236c757d%22 width=%22200%22 height=%22180%22/%3E%3Ctext fill=%22%23fff%22 x=%22100%22 y=%2290%22 dominant-baseline=%22middle%22 text-anchor=%22middle%22 font-size=%2212%22%3ENo image%3C/text%3E%3C/svg%3E';">
                <% } else { %>
                  <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 180px;">
                    <span class="text-white-50"><%= ship.name %></span>
                  </div>
                <% } %>
                <div class="card-body">
                  <h6 class="card-title"><%= ship.name %></h6>
                  <p class="card-text small text-muted"><%= ship.class ? ship.class.type : '' %> · <%= ship.yearCommissioned %></p>
                  <a href="/gallery/image/<%= ship.id %>" target="_blank" class="btn btn-outline-dark btn-sm w-100">View Full Size</a>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <div class="col-12 text-center text-muted py-5">No images available. Ensure the API is running.</div>
        <% } %>
      </div>
    </div>
  </main>
  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
