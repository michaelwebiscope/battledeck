<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>WW2 Naval History Archive</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=Source+Sans+3:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root { --navy-dark: #1a2332; --navy-mid: #2c3e50; --cream: #f8f6f1; --parchment: #e8e4dc; --accent: #8b7355; }
  body { font-family: 'Source Sans 3', sans-serif; background-color: var(--cream); color: #333; min-height: 100vh; display: flex; flex-direction: column; }
  h1, h2, h3, .display-4, .display-5 { font-family: 'Cormorant Garamond', serif; color: var(--navy-dark); }
  .navbar-brand { font-family: 'Cormorant Garamond', serif; }
  .card { border: none; border-radius: 4px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
  .card-header { background: var(--navy-dark); color: white; font-family: 'Cormorant Garamond', serif; font-weight: 600; }
  main { flex: 1; }
  .navbar { background: var(--navy-dark) !important; }
  .navbar-toggler { color: rgba(255,255,255,0.9); }
  .navbar-toggler:focus { box-shadow: 0 0 0 0.15rem rgba(255,255,255,0.4); }
  #navMain { background: var(--navy-dark); }
  #navMain .nav-link { color: rgba(255,255,255,0.9); padding: 0.5rem 1rem; }
  #navMain .nav-link:hover, #navMain .nav-link.active { color: #fff; background: rgba(255,255,255,0.1); }
</style>
