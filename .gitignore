# Infra/deployment (not required to run app)
docs/
puppet/
newrelic/
NavalArchive.ImagePopulator/.newrelic-java/

# Database
*.db
*.db-shm
*.db-wal

# .NET
bin/
obj/
*.user
*.suo
*.cache
*.dll
*.pdb
*.log
.vs/
packages/

# Video files (place ship-{id}.mp4 manually)
NavalArchive.VideoService/videos/*.mp4
NavalArchive.VideoService/videos/*.webm

# Node
node_modules/
npm-debug.log*

# Cloudflare Origin certs (place real files in certs/; .example files are tracked)
certs/cloudflare-origin.pem
certs/cloudflare-origin-key.pem
certs/*.pfx

# Terraform
terraform-navalansible/.terraform/
terraform-navalansible/*.tfstate
terraform-navalansible/*.tfstate.backup
terraform-navalansible/*.tfvars

# OS
.DS_Store
Thumbs.db
