---
# Split so staged deploy can run install + configure as separate WinRM sessions.
- name: Include PostgreSQL install
  ansible.builtin.include_tasks: postgres_install.yml

- name: Include PostgreSQL configure
  ansible.builtin.include_tasks: postgres_configure.yml
