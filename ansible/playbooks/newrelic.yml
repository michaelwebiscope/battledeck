---
# Backward-compatible alias: full NR stack (infra + logs + .NET → java → go → otel → node).
# Prefer referencing newrelic-dotnet-java-go.yml directly in new scripts.
- ansible.builtin.import_playbook: newrelic-dotnet-java-go.yml
