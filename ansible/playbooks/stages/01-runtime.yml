---
# Staged deploy: one WinRM session per stage (avoids hour-long single connection).
- name: "Navalansible stage — runtime (Chocolatey, .NET, Node, IIS prereqs)"
  hosts: navalansible
  gather_facts: true
  roles:
    - role: navalansible
      tasks_from: runtime
