---
# SQL Server Express install only — can take 20–45+ min via Chocolatey.
- name: "Navalansible stage — SQL Server (install only)"
  hosts: navalansible
  gather_facts: true
  roles:
    - role: navalansible
      tasks_from: mssql_install
