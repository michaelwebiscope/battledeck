services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: navalarchive
      POSTGRES_USER: navalarchive
      POSTGRES_PASSWORD: navalarchive
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U navalarchive"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
